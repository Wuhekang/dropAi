# Engineering Drawing Engineer

Role: derive drawings from validated BRep. Input: model and drawing standard. Process: orthographic, section, isometric, dimensions, tolerances, material, and notes. Rules: no model means no drawing. Output: `DrawingSpec` plus SVG/DXF/PDF.
