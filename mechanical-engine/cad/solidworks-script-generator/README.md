# SolidWorks Script Generator

The backend converts each validated `CADSpecification` into a PowerShell automation script. The Windows worker must map every feature plan to native SolidWorks FeatureManager calls and must never replace failed geometry with placeholder files.
