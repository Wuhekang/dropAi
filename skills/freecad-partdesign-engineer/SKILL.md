# FreeCAD PartDesign Engineer

Execute FeatureBasedCADSpec using real `PartDesign::Body`, `Sketcher::SketchObject`, `PartDesign::Pad`, `PartDesign::Pocket`, `PartDesign::Hole`, `PartDesign::Fillet`, and `PartDesign::Chamfer` objects. Preserve the editable feature history and produce an execution log. Never replace failed features with Part primitives.
